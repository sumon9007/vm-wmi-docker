# Project Rules

## Directory Structure

- **scripts/**: All shell scripts (`.sh`) must be stored in this directory.
- **config/**: Configuration files (e.g., `docker-compose.yml`, `nginx.conf`, env files).
- **data/**: Persistent data storage (e.g., database volumes).
- **logs/**: Application and system logs.
- **docs/**: Project documentation and manuals.

# Docker Setup Script Walkthrough

I have created a shell script to automate the installation of Docker on your Ubuntu system.

## Changes Created
- [git-setup.sh](file:///home/wmiadmin/scripts/git-setup.sh): Helper script to:
  - Install Git (if missing)
  - Configure `user.name` and `user.email`
  - Generate SSH keys for GitHub
- [docker-setup.sh](file:///home/wmiadmin/scripts/docker-setup.sh): A bash script that handles:
  - Repository configuration (Official Docker Repo)
  - Keyring management (GPG keys)
  - Package installation (Docker CE, CLI, plugins)
  - User permissions (Adding current user to `docker` group)
- [README.md](file:///home/wmiadmin/README.md): Documented the new project directory structure.

## New Directories
- `scripts/`: For shell scripts.
- `config/`: For configuration files.
- `data/`: For persistent data.
- `logs/`: For logs.
- `docs/`: For documentation.

## GitHub Repository
- **Repository**: [vm-wmi-docker](https://github.com/sumon9007/vm-wmi-docker)
- **Status**: Initialized and pushed to GitHub.
- **Ignored Files**: Configured in [.gitignore](file:///home/wmiadmin/.gitignore).

## Verification
I have verified the script syntax and permissions:
- Script is executable (`chmod +x` applied).
- Script follows official Docker documentation for Ubuntu.

## Next Steps for You
1. Run the script:
   ```bash
   ./docker-setup.sh
   ```
2. Log out and log back in to apply group changes.
3. Verify installation:
   ```bash
   docker run hello-world
   ```
